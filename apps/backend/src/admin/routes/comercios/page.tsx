import { defineRouteConfig } from "@medusajs/admin-sdk"
import ComerciosPage from "../../components/ComerciosPage"

export const config = defineRouteConfig({ label: "Comercios" })

export default ComerciosPage
